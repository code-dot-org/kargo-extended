When adding a new proposal under `extended/docs/proposals/`, read `0000-proposal-directory-structure/proposal.md` first and follow it.
When changing a proposal's status, update both `proposal.md` and `status.yaml` if `status.yaml` exists.
